<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hotelreservation.mapper.ReserveMapper">
    <insert id="reserve_room" parameterType="string">
        INSERT INTO `reservation` VALUES(NULL, #{userId}, #{roomNo}, #{reservationDate}, #{price}, #{state})
    </insert>
</mapper>